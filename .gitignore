.DS_Store
*.swp
*.swo
.idea/
.vscode/
node_modules/
__pycache__/

# Local-only evaluation harness. Kept out of the published repo.
evals/

# Local Claude Code state. Machine-specific; never publish.
.claude/

# download/ is deliberately NOT ignored. Those ZIPs are the no-terminal install
# route — people download them straight from GitHub — so they have to be in the
# repo. CI rebuilds them and fails if they drift from skills/.
